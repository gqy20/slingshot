# Slingshot 视频制作约定

本文件记录项目在 EP03–EP05 制作中形成的共同判断、审美偏好与验收方法。后续代理在策划、实现、审查或渲染 Episode 时，应先遵守这里的约定，再参考 `docs/episode-authoring.md`、具体分镜和 Episode JSON。

目标不是套用一套僵硬模板，而是持续制作：科学上诚实、叙事上像共同讨论、画面克制而有运动感、在手机端也能轻松读懂的短科普视频。

## 1. 先理解再修改

- 开始工作前先阅读相关 Episode JSON、讲稿、分镜、相邻集数和已有测试；涉及风格时，优先把 EP04 当作成熟基准，把 EP05 当作长篇结构、赛道动画和讨论式旁白的最新基准。
- 尊重工作区中的现有改动。不要覆盖、回退或顺手整理与当前任务无关的文件；`PROFILE` 是用户维护的文件，除非用户明确要求，否则不得修改。
- 先修根因，不为单张截图堆坐标补丁。文字遮挡通常是布局职责、可用区域或时间状态的问题，应检查整个 Beat，而不只检查截到的那一帧。
- 保持架构适度集中。只有存在两个以上真实复用者、独立生命周期或明确测试边界时才拆出新模块；单一镜头的少量绘制逻辑留在对应 Canvas 内，不要把一个视觉概念打散成许多微型文件。
- 共享能力放在 core、video、通用布局和解释模块中；某一课程或 Episode 独有的物理模型、文案和镜头留在自己的 domain/content/Canvas 中。共享的是协议和工具，不共享偶然细节。

## 2. 选题品味

优先选择同时具备以下特征的题目：

- 一句话就能形成清晰冲突，但冲突不是标题党；例如“最短的路为什么不一定最快”。
- 结论能由画面亲自证明，观众不依赖大段口述或公式才看懂。
- 至少有一个连续的因果链，而不只是冷知识答案。
- 能在 Godot 的二维程序化动画中诚实实现；需要局部放大、时间压缩或参考系变化时，必须明确标注。
- 有清楚的模型边界和自然延伸问题，结尾可以继续思考，而不是强行制造悬念。
- 相比常见科普内容，有新的观察角度、呈现机制或数据证据。

选题资料与候选池写入 `docs/topics/`，不要把调研笔记塞进 Episode 代码。调研时优先使用论文、大学课程、科研机构或官方资料；区分“物理事实”“为了呈现做的时间映射”和“创作推断”。

## 3. 分镜先于讲稿，时长由内容决定

- 先确定每个 Beat 的问题、唯一视觉判断、主体、进入方式和退出方式，再写旁白。
- 一个镜头只承担一个主要判断。公式、结果表、图例、标题和主动画不要同时争夺注意力。
- 时间线不能先机械锁死再往里塞字幕。先估算旁白真实朗读时长、必要停顿、动画建立与读图时间，再反推 Beat 时长。
- 3–4 分钟的正式 Episode 通常以约 3 分 40 秒为舒适目标，并始终低于 4 分钟；内容较短时不注水，内容需要更长时先删重复解释，而不是提高语速。
- 旁白结束后要给重要视觉结论留下可观察时间；画面已经说明的内容，旁白不重复逐项念一遍。
- 画面锁定之后才生成正式配音和字幕。若重写讲稿改变了真实音频时长，应重新对齐时间线，而不是把音频硬压进旧时长。

每个 Beat 至少回答：

1. 观众此刻应该看哪里？
2. 这一镜新增了什么理解？
3. 画面本身能否支持旁白所说的话？
4. 文字消失后，因果关系是否仍然可见？
5. 进入下一镜的视觉或语义动机是什么？

## 4. 开场与品牌节奏

- 冷启动先给现象、运动或矛盾，不用主持人口吻要求观众“下注”。
- 开场问题应从刚刚看到的事实自然生长出来；不要先宣布答案，也不要用空洞的“你猜”“反转来了”代替问题。
- Logo/品牌短片不能遗漏，但也不要挡住第一秒的有效钩子。通常在冷开场已经形成问题之后，用短而完整的 brand stinger 作为呼吸和章节门槛，再进入解释。
- 核对冷启动的完整节奏：第一帧是否立即有信息、运动是否建立得太慢、切黑是否过长、Logo 是否突兀、Logo 后是否重复开场、前 30 秒是否出现不必要的长公式卡。
- Logo、标题和主体动画应串联出现，避免三者同一时刻抢占中心。

## 5. 解说：共同讨论，不是短视频主持

旁白的基本语气是：和观众一起观察一个问题，然后逐步建立判断。它应准确、自然、有思考感，不卖关子，不喊口号，不把观众当作答题对象。

推荐结构：

1. 描述此刻共同看见的事实；
2. 指出事实之间的张力；
3. 引入恰好够用的概念或模型；
4. 回到画面验证；
5. 说明结论适用到哪里，并留下真正相关的下一问。

避免以下表达习惯：

- “先别猜”“先别算”“你押谁”“还押直线吗”；
- “答案锁定”“获胜曲线”“最短路径输了”“反转来了”；
- 无证据的“竟然、居然、震惊、颠覆认知”；
- 把实验写成赛事解说，频繁使用“抢跑、领先、绝杀”；
- 连续的设问句、命令句和故意吊胃口；
- 逐字朗读公式、表格和屏幕上已经完整显示的文字；
- 为了显得亲切而加入没有信息量的口头禅。

更合适的写法是“从几何距离看，直线确实最短。不过，小球比较的不是走了多少米，而是用了多少时间。”这种陈述允许观众参与推理，但不强迫互动。

写完讲稿后全文扫描上述词汇及其同类表达，并通读段落之间是否像一场连续讨论。不要只逐句润色；尤其检查相邻 Beat 是否重复提出同一个问题。

## 6. 配音配置不可随意改动

- 语速是声音表演的一部分，不是填满时间线的伸缩参数。调整时长优先改文案、停顿和 Beat，而不是擅自改语速。
- 已确认基准：EP03 为 `Chinese (Mandarin)_Sincere_Adult`、速度 `1.08`；EP04 为 `Chinese (Mandarin)_Warm_Bestie`、速度 `1.0`；EP05 为 `Chinese (Mandarin)_Warm_Bestie`、速度 `1.2`。
- 已经确认的 model、voice、speed、volume、pitch、emotion 和发音表都应原样保留，除非用户明确要求重新设计声音。
- 使用 `<#x#>` 控制有意义的思考停顿，不用大量停顿掩盖句子结构问题。
- 生成正式音频后，以实际时长和听感验收；文本字符数不能代替语音时长。
- 音频继续满足项目规定的响度、真峰值、采样率与 manifest 记录要求。

## 7. 字幕是版面的一部分

- 字幕必须与实际解说同步，而不只是与 Beat 大致同步。旁白文本、SRT 正文和最终烧录版本要一致。
- 按语义短语和标点断句，不在姓名、颜色、术语或动宾结构中间硬切。中文每条字幕优先一行，必要时最多两行。
- 字幕持续时间要足够阅读；短句不要闪烁，长句不要一次铺满屏幕。
- 1080p 预览以项目默认约 42 px 字号和约 100 px 底边距为起点，但最终由实际画面验收决定。不要为了塞入长句缩小字号，应先重写或拆分句子。
- 主图、终点、坐标轴、脚注和结果面板不得侵入字幕安全区。必要时整体上移物理画面，而不是把字幕继续压低。
- 审查时使用已经烧录字幕的成片，并考虑播放器控制栏会暂时遮住最底部区域。
- Windows PowerShell 5 读取无 BOM UTF-8 脚本时可能破坏中文正则；脚本中的中文标点匹配优先使用 Unicode 转义，例如 `\u3002`、`\uFF0C`，并明确指定 UTF-8 读写。

## 8. 排版、字号与布局

- 使用 `assets/video_typography.tres` 中已有的语义角色，不在 Canvas 内随手创建临时字号。
- EP04 是字号层级、字幕清晰度和克制信息密度的最低基准；新 Episode 不应因为内容更多而整体缩小文字。
- 手机端可读性优先。Meta 信息可以较小，但任何承担结论、条件、数值比较或操作提示的文字都必须一眼可读。
- 标题、正文、数据和公式之间必须有明显层级；一张卡片不应出现多个同等响亮的标题。
- 面板尺寸应由内容、内边距和对齐关系决定，不用固定大框包少量信息。避免框线碰字、内容偏在一角、卡片看似未完成。
- 先划分标题区、主动画区、数据/解释区和字幕安全区，再放元素。不要让每个组件只根据全屏坐标独立定位。
- 检查的不只是静态矩形是否相交，还包括运动轨迹、淡入淡出、数字增长、镜头缩放和局部放大过程中的占位变化。
- 装饰元素必须退后：低对比网格、框线和轨迹不能穿过主要文字造成可读性下降。

## 9. 公式统一使用 Typst

- 所有正式数学公式使用 Typst 生成的透明 SVG；Godot 文本只作为资产缺失时的无障碍兜底。不要用普通 Label 拼接分式、根号、上下标或积分上下限。
- 使用项目固定的 Typst 版本和数学字体，通过现有公式资产构建脚本生成，不手工编辑生成 SVG。
- 公式应由正在发生的物理现象引出：先出现高度、速度、路程小段或力的标注，再收束为完整公式。
- 公式卡必须为 SVG 实际墨迹预留上、下、左、右安全边距。特别检查积分号、根号、分数、上标、重音符号和积分上限等高字形。
- 如果公式上半部或字符（例如积分上限 `B`）被裁切，应从 SVG `viewBox`、Typst 导出边界、TextureRect 的拉伸模式、容器裁剪和布局高度逐层定位；不能只把公式整体向下挪来掩盖裁切。
- 公式与上方说明、下方结论之间要有可见呼吸，不允许公式的视觉边界紧贴相邻文字。
- 最终核对 1080p 实际渲染，而不是只看源 SVG 或编辑器预览。

## 10. 动画：丰富来自因果，不来自堆叠

- 每个重要概念都应有一个可见动作：形成、比较、迁移、累积、放大、锁定或回到原场景。
- 同一时刻优先保留一个主动作和一个辅助读数。镜头、主体、公式、面板和装饰不要一起动。
- 用对象连续性建立优雅感：同一颗球、同一段轨迹、同一个颜色和同一数值应跨镜头保持身份，不靠反复切新卡片解释。
- 数值变化应与物理状态同步；不要为了热闹加入与模型无关的漂浮、弹跳或粒子。
- 慢放、局部放大和时间压缩必须标明是“回放”“局部放大”或真实时间倍率，同时保留真实时间证据。
- 重要差异如果肉眼不可见，应设计诚实的放大机制，而不是篡改物理结果让差异变大。
- 给入场、停留和离场完整节奏。文字和图表不应永远悬挂在画面上；完成任务后应退出，为主体让路。

判断动画是否“足够”的标准不是每秒有多少元素运动，而是：静音观看时，观众能否跟随注意力看懂因果链；暂停任意密集帧时，画面是否仍有清楚的第一、第二阅读层级。

## 11. 科学呈现

- 先展示证据，再给结论；先说明比较条件，再比较结果。
- 明确区分路径长度、瞬时速度、累计时间等不同物理量，不用相似的视觉编码暗示它们等价。
- 公式只承担必要解释，不把完整推导塞进一集。每一步保持一个概念、一条关系和一句白话结论。
- 模型假设应在结论附近明确出现，例如无摩擦、质点、匀强重力、忽略空气阻力；不能只藏在片尾小字里。
- 数据必须来自求解器或确定性 RunRecord，不为配合旁白手填近似动画状态。显示值可按合理精度格式化，但排名和时间差必须与记录一致。
- 物理模拟、镜头时间映射和装饰动画分层；视觉慢放不能改变记录中的真实到达时间。

## 12. 渲染策略

- 默认使用无头模式，避免 Godot 窗口反复弹出。只有用户明确需要观看实时窗口或排查图形驱动时才使用 `-ShowRenderWindow`。
- Windows 默认 `2` 个 Render Worker，这是当前设备实测后最稳定、收益最明确的设置。脚本允许 1–4 Worker，但不要因为“更多更快”擅自把默认值改为 3 或 4。
- 优先使用 Beat 帧缓存。只修改讲稿、字幕、配音或混音时，应复用画面缓存或只重烧录，不重渲染全部画面。
- 视觉开发顺序：单 Beat/关键时间短预览 → 1080p 完整预览 → 视觉回归 → 最终 4K。不要每次小改都直接进行完整 4K 渲染。
- 分片必须按稳定的 Beat 边界规划，保留 warmup，且任意 Worker 从绝对帧启动都应得到相同状态。
- 需要可比性能数据时，固定分辨率、编码器、缓存状态和素材版本；不要把缓存命中与 Worker 加速混在同一结论里。

常用命令：

```powershell
# 生成或更新正式配音、标准化音频与字幕
.\scripts\generate_narration.ps1 content\episodes\<episode-id>.json

# 默认无头、2 Worker 的 1080p 完整预览
.\scripts\render_episode.ps1 content\episodes\<episode-id>.json `
  -Width 1920 -Height 1080 -RenderWorkers 2 -SkipImport

# 自动测试
.\scripts\run_tests.ps1

# 包含视觉回归的测试
.\scripts\run_tests.ps1 -Visual
```

## 13. 审查必须覆盖时间，而不是只看一张图

每次视觉修改至少完成以下检查：

1. 检查目标 Beat 的早期、中段、末段，以及文字和动画同时存在的最密集时刻；
2. 检查前后 Beat 切换，确认旧 HUD、标题、卡片或 Logo 没有残留；
3. 在 1080p 烧录字幕的预览中检查标题、主体、数据、公式和字幕重叠；
4. 对公式查看原始分辨率截图，检查字形是否裁切，而不只看缩略图；
5. 静音观看关键段落，检查动画是否独立表达因果；
6. 只听音频或阅读 SRT，检查讨论语气、重复解释、断句和停顿；
7. 跑自动测试和视觉回归，再生成完整预览；
8. 使用 ffprobe/manifest 核对总时长、分辨率、帧率、音轨和实际 Worker/缓存状态。

不要仅以“测试通过”宣称画面已核对。自动布局审计只能发现已编码的规则，仍需人工查看真实成片中的视觉重量、运动交叉和播放器环境。

## 14. 完成标准

一个 Episode 可以交付，至少需要满足：

- 开场现象、问题和品牌短片节奏完整；
- 每个 Beat 有唯一主要判断，画面和解说在同一时刻谈同一件事；
- 总时长由内容自然形成，目标约 3 分 40 秒且不超过 4 分钟；
- 解说是连续讨论感，没有主持人下注、赛事化和廉价反转话术；
- 配音沿用已确认配置，字幕断句自然并与音频同步；
- 公式全部由 Typst 生成，无裁切，字号和上下间距足够；
- 1080p 下不存在文字与动画、字幕与主体、面板与轨迹的明显重叠；
- 动画能在静音时表达主要因果，慢放和放大标注诚实；
- 模型边界、数据来源和真实时间关系清楚；
- 自动测试、视觉回归、关键帧人工检查和完整预览均已完成；
- 最终回复明确说明改了什么、验证了什么、成片路径和尚未完成的事项，不把未执行的检查说成已完成。
